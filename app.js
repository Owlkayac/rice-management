const LOW_STOCK_THRESHOLD=100,INVENTORY_STORAGE_KEY="inventory",SHIPMENTS_STORAGE_KEY="shipments",CUSTOMERS_STORAGE_KEY="customers";
const varieties=["A","B","C","D","E","F"],months=Array.from({length:12},(_,i)=>`${i+1}月`);
let reservations=read("reservations",[]),shipments=read(SHIPMENTS_STORAGE_KEY,[]),customers=read(CUSTOMERS_STORAGE_KEY,[]),inventory=loadInventory();let editingIndex=null,editingShipmentIndex=null,editingCustomerId=null;
function read(k,f){try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}}function save(k,v){localStorage.setItem(k,JSON.stringify(v))}function formatKg(v){return `${Math.round((Number(v)||0)*10)/10}kg`}function uid(){return `customer-${Date.now()}-${Math.random().toString(36).slice(2,8)}`}function loadInventory(){const x=read(INVENTORY_STORAGE_KEY,{}),r={};varieties.forEach(v=>r[v]=Number.isFinite(Number(x[v]))&&Number(x[v])>=0?Number(x[v]):0);return r}
function fillOptions(){["variety","shipmentVariety"].forEach(id=>{const e=document.getElementById(id);e.innerHTML=varieties.map(v=>`<option>${v}</option>`).join("")});document.getElementById("filterVariety").innerHTML='<option value="">すべて</option>'+varieties.map(v=>`<option>${v}</option>`).join("");document.getElementById("month").innerHTML=months.map(m=>`<option>${m}</option>`).join("");document.getElementById("filterMonth").innerHTML='<option value="">すべて</option>'+months.map(m=>`<option>${m}</option>`).join("");refreshCustomerSelects()}
function customerFor(item){return item.customerId?customers.find(c=>c.customerId===item.customerId):customers.find(c=>c.name===item.name)}function customerName(item){return customerFor(item)?.name||item.name||"未登録"}function refreshCustomerSelects(){const opts='<option value="">顧客管理から登録してください</option>'+customers.map(c=>`<option value="${c.customerId}">${esc(c.name)}</option>`).join("");["customerSelect","shipmentCustomerSelect"].forEach(id=>{const e=document.getElementById(id),old=e.value;e.innerHTML=opts;if(customers.some(c=>c.customerId===old))e.value=old})}
function esc(s){return String(s??"").replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function getFormValues(){const s=document.getElementById("customerSelect"),legacy=document.getElementById("name").value.trim();return{variety:variety.value,month:month.value,name:legacy||customers.find(c=>c.customerId===s.value)?.name||"",customerId:s.value||undefined,kg:Number(document.getElementById("kg").value)}}function addReservation(){const r=getFormValues();if(!r.name){alert("顧客を選択するか、既存データ用の名前を入力してください");return}if(!r.kg||r.kg<=0){alert("kgを入力してください");return}if(editingIndex===null)reservations.push(r);else{reservations[editingIndex]=r;cancelEdit()}save("reservations",reservations);clearReservation();refreshAll()}function editReservation(i){const r=reservations[i];editingIndex=i;document.getElementById("variety").value=r.variety;document.getElementById("month").value=r.month;document.getElementById("name").value=r.name||"";document.getElementById("kg").value=r.kg;document.getElementById("customerSelect").value=r.customerId||customerFor(r)?.customerId||"";document.getElementById("submitButton").textContent="変更を保存";document.getElementById("cancelEditButton").hidden=false}function cancelEdit(){editingIndex=null;clearReservation();document.getElementById("submitButton").textContent="予約を追加";document.getElementById("cancelEditButton").hidden=true}function clearReservation(){document.getElementById("name").value="";document.getElementById("kg").value="";document.getElementById("customerSelect").value=""}function deleteReservation(i){if(confirm("この予約を削除しますか？")){reservations.splice(i,1);save("reservations",reservations);refreshAll()}}
function totals(list,filter){const r={};list.filter(filter||(()=>true)).forEach(x=>r[x.variety]=(r[x.variety]||0)+(Number(x.kg)||0));return r}function getReservedTotals(){return totals(reservations)}function getShippedTotals(){return totals(shipments)}function displayReservations(){const q=document.getElementById("searchName").value.trim(),fv=document.getElementById("filterVariety").value,fm=document.getElementById("filterMonth").value,body=document.getElementById("reservationList");body.innerHTML="";reservations.forEach((r,i)=>{if(q&&!customerName(r).includes(q)&&!(r.name||"").includes(q)||fv&&r.variety!==fv||fm&&r.month!==fm)return;const tr=document.createElement("tr");[r.variety,r.month,customerName(r),formatKg(r.kg)].forEach(v=>{const td=document.createElement("td");td.textContent=v;tr.appendChild(td)});const td=document.createElement("td");td.className="action-cell";td.innerHTML='<button class="edit-button">編集</button><button class="delete-button">削除</button>';td.children[0].onclick=()=>editReservation(i);td.children[1].onclick=()=>deleteReservation(i);tr.appendChild(td);body.appendChild(tr)});const sums={};reservations.forEach(r=>{const k=`${r.variety}_${r.month}`;sums[k]=(sums[k]||0)+(Number(r.kg)||0)});document.getElementById("summary").innerHTML=Object.entries(sums).map(([k,v])=>`<div class="summary-item">${k.replace("_","　")}　${formatKg(v)}</div>`).join("")}
function displayDashboard(){const data={};varieties.forEach(v=>data[v]={});reservations.forEach(r=>data[r.variety]&&(data[r.variety][r.month]=(data[r.variety][r.month]||0)+(Number(r.kg)||0)));let grand=0,html="";varieties.forEach(v=>{let sum=0;html+=`<tr><th>${v}</th>`+months.map(m=>{const n=data[v][m]||0;sum+=n;return`<td>${formatKg(n)}</td>`}).join("")+`<td>${formatKg(sum)}</td></tr>`;grand+=sum});html+=`<tr class="grand-total-row"><th>全体</th>${months.map(m=>`<td>${formatKg(reservations.filter(r=>r.month===m).reduce((a,r)=>a+(Number(r.kg)||0),0))}</td>`).join("")}<td>${formatKg(grand)}</td></tr>`;document.getElementById("dashboardTableBody").innerHTML=html;document.getElementById("dashboardTotal").textContent=formatKg(grand);const rc=new Set(reservations.map(r=>customerFor(r)?.customerId||`name:${r.name}`)),sc=new Set(shipments.map(r=>customerFor(r)?.customerId||`name:${r.name}`));document.getElementById("dashboardCustomerCount").textContent=customers.length;document.getElementById("dashboardReservationCustomerCount").textContent=rc.size;document.getElementById("dashboardShipmentCustomerCount").textContent=sc.size;document.getElementById("dashboardUnshippedTotal").textContent=formatKg(grand-getShippedTotalsAll())}
function getShippedTotalsAll(){return shipments.reduce((a,s)=>a+(Number(s.kg)||0),0)}function displayInventory(){const reserved=getReservedTotals(),shipped=getShippedTotals(),body=document.getElementById("inventoryList");body.innerHTML="";varieties.forEach(v=>{const remain=inventory[v]-shipped[v],tr=document.createElement("tr");tr.className=remain<0?"stock-shortage":remain<LOW_STOCK_THRESHOLD?"stock-low":"";tr.innerHTML=`<th>${v}</th><td><input type="number" min="0" value="${inventory[v]}"></td><td>${formatKg(reserved[v])}</td><td>${formatKg(remain)}</td><td>${remain<0?"在庫不足":remain<LOW_STOCK_THRESHOLD?"在庫少":""}</td>`;tr.querySelector("input").onchange=e=>{inventory[v]=Math.max(0,Number(e.target.value)||0);save(INVENTORY_STORAGE_KEY,inventory);refreshAll()};body.appendChild(tr)})}
function shipmentValues(){const s=document.getElementById("shipmentCustomerSelect"),legacy=document.getElementById("shipmentName").value.trim();return{variety:shipmentVariety.value,date:shipmentDate.value,name:legacy||customers.find(c=>c.customerId===s.value)?.name||"",customerId:s.value||undefined,kg:Number(shipmentKg.value),memo:shipmentMemo.value.trim()}}function addShipment(){const s=shipmentValues();if(!s.date||!s.name||!s.kg||s.kg<=0){alert("出荷日・顧客・出荷kgを入力してください");return}if(editingShipmentIndex===null)shipments.push(s);else{shipments[editingShipmentIndex]=s;cancelShipmentEdit()}save(SHIPMENTS_STORAGE_KEY,shipments);clearShipmentForm();refreshAll()}function editShipment(i){const s=shipments[i];editingShipmentIndex=i;shipmentVariety.value=s.variety;shipmentDate.value=s.date;shipmentName.value=s.name||"";shipmentKg.value=s.kg;shipmentMemo.value=s.memo||"";shipmentCustomerSelect.value=s.customerId||customerFor(s)?.customerId||"";shipmentSubmitButton.textContent="変更を保存";cancelShipmentEditButton.hidden=false}function cancelShipmentEdit(){editingShipmentIndex=null;clearShipmentForm();shipmentSubmitButton.textContent="出荷を登録";cancelShipmentEditButton.hidden=true}function clearShipmentForm(){["shipmentDate","shipmentName","shipmentKg","shipmentMemo"].forEach(id=>document.getElementById(id).value="");shipmentCustomerSelect.value=""}function deleteShipment(i){if(confirm("この出荷データを削除しますか？")){shipments.splice(i,1);save(SHIPMENTS_STORAGE_KEY,shipments);refreshAll()}}function displayShipments(){const b=document.getElementById("shipmentList");b.innerHTML="";shipments.forEach((s,i)=>{const tr=document.createElement("tr");[s.date,s.variety,customerName(s),formatKg(s.kg),s.memo||""].forEach(v=>{const td=document.createElement("td");td.textContent=v;tr.appendChild(td)});const td=document.createElement("td");td.innerHTML='<button class="edit-button">編集</button><button class="delete-button">削除</button>';td.children[0].onclick=()=>editShipment(i);td.children[1].onclick=()=>deleteShipment(i);tr.appendChild(td);b.appendChild(tr)});const r=getReservedTotals(),s=getShippedTotals(),body=document.getElementById("shipmentSummaryBody");body.innerHTML=varieties.map(v=>`<tr><th>${v}</th><td>${formatKg(inventory[v])}</td><td>${formatKg(r[v])}</td><td>${formatKg(s[v])}</td><td>${formatKg(r[v]-s[v])}</td></tr>`).join("")}
function customerStats(c){const rs=reservations.filter(r=>customerFor(r)?.customerId===c.customerId),ss=shipments.filter(s=>customerFor(s)?.customerId===c.customerId),byV=totals(rs),shipV=totals(ss),byM=totals(rs,r=>true);const month={};rs.forEach(r=>month[r.month]=(month[r.month]||0)+(Number(r.kg)||0));const reserved=rs.reduce((a,r)=>a+(Number(r.kg)||0),0),shipped=ss.reduce((a,s)=>a+(Number(s.kg)||0),0);return{rs,ss,byV,shipV,month,reserved,shipped,unshipped:reserved-shipped}}
function displayCustomers(){const q=document.getElementById("customerSearch").value.trim(),sort=document.getElementById("customerSort").value,arr=customers.filter(c=>[c.name,c.phone,c.address].join(" ").includes(q)).map(c=>({c,s:customerStats(c)}));arr.sort((a,b)=>sort==="reservations"?b.s.reserved-a.s.reserved:a.c.name.localeCompare(b.c.name,"ja"));document.getElementById("customerList").innerHTML=arr.map(({c,s})=>`<tr><td>${esc(c.name)}</td><td>${esc(c.phone)}</td><td>${esc(c.address)}</td><td>${esc(c.memo)}</td><td>${formatKg(s.reserved)}</td><td>${formatKg(s.shipped)}</td><td>${s.unshipped<=0?"出荷完了":formatKg(s.unshipped)}</td><td><button class="detail-button" onclick="showCustomerDetail('${c.customerId}')">詳細</button></td><td><button class="edit-button" onclick="editCustomer('${c.customerId}')">編集</button></td><td><button class="delete-button" onclick="deleteCustomer('${c.customerId}')">削除</button></td></tr>`).join("")}
function saveCustomer(){const name=customerName.value.trim();if(!name){alert("顧客名を入力してください");return}const c={customerId:editingCustomerId||uid(),name,phone:customerPhone.value.trim(),address:customerAddress.value.trim(),memo:customerMemo.value.trim()};if(editingCustomerId)customers=customers.map(x=>x.customerId===editingCustomerId?c:x);else customers.push(c);save(CUSTOMERS_STORAGE_KEY,customers);cancelCustomerEdit();refreshAll()}function editCustomer(id){const c=customers.find(x=>x.customerId===id);editingCustomerId=id;customerName.value=c.name;customerPhone.value=c.phone||"";customerAddress.value=c.address||"";customerMemo.value=c.memo||"";customerSubmitButton.textContent="変更を保存";cancelCustomerEditButton.hidden=false;switchView("customersView")}function cancelCustomerEdit(){editingCustomerId=null;["customerName","customerPhone","customerAddress","customerMemo"].forEach(id=>document.getElementById(id).value="");customerSubmitButton.textContent="顧客を登録";cancelCustomerEditButton.hidden=true}function deleteCustomer(id){const c=customers.find(x=>x.customerId===id),s=customerStats(c);if(s.rs.length||s.ss.length){alert("この顧客には予約または出荷データが存在します。顧客情報を削除すると関連付けが失われる可能性があります。関連データを先に確認してください。");return}if(confirm(`${c.name}を削除しますか？`)){customers=customers.filter(x=>x.customerId!==id);save(CUSTOMERS_STORAGE_KEY,customers);refreshAll()}}
function showCustomerDetail(id){const c=customers.find(x=>x.customerId===id),s=customerStats(c),list=o=>Object.entries(o).map(([k,v])=>`<li>${k}：${formatKg(v)}</li>`).join("")||"<li>なし</li>";const d=document.getElementById("customerDetail");d.hidden=false;d.innerHTML=`<h2>${esc(c.name)} の詳細</h2><div class="detail-grid"><div class="detail-card"><p><b>電話番号：</b>${esc(c.phone)||"未登録"}</p><p><b>住所：</b>${esc(c.address)||"未登録"}</p><p><b>メモ：</b>${esc(c.memo)||"なし"}</p></div><div class="detail-card"><h3>取引状況</h3><p>予約合計：${formatKg(s.reserved)}</p><p>出荷済み：${formatKg(s.shipped)}</p><p>未出荷：${s.unshipped<=0?"出荷完了":formatKg(s.unshipped)}</p></div><div class="detail-card"><h3>予約（品種別）</h3><ul>${list(s.byV)}</ul></div><div class="detail-card"><h3>予約（月別）</h3><ul>${list(s.month)}</ul></div><div class="detail-card"><h3>出荷（品種別）</h3><ul>${list(s.shipV)}</ul></div></div><button onclick="document.getElementById('customerDetail').hidden=true">詳細を閉じる</button>`;d.scrollIntoView({behavior:"smooth"})}
function switchView(id){document.querySelectorAll(".view-panel").forEach(e=>e.hidden=e.id!==id);document.querySelectorAll(".view-tab").forEach(e=>e.classList.toggle("active",e.dataset.view===id));if(id==="customersView")displayCustomers()}function refreshAll(){displayReservations();displayDashboard();displayInventory();displayShipments();displayCustomers();refreshCustomerSelects()}
fillOptions();["searchName","filterVariety","filterMonth","customerSearch","customerSort"].forEach(id=>document.getElementById(id).addEventListener("input",refreshAll));document.getElementById("filterVariety").onchange=refreshAll;document.getElementById("filterMonth").onchange=refreshAll;document.getElementById("customerSort").onchange=displayCustomers);document.getElementById("customerSelect").onchange=e=>{document.getElementById("name").value=customers.find(c=>c.customerId===e.target.value)?.name||""};document.getElementById("shipmentCustomerSelect").onchange=e=>{document.getElementById("shipmentName").value=customers.find(c=>c.customerId===e.target.value)?.name||""};document.querySelectorAll(".view-tab").forEach(e=>e.onclick=()=>switchView(e.dataset.view));refreshAll();
